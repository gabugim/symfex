{% extends 'base.html.twig' %}
{% block title %}Actors {% endblock %}

{% block body %}
<h1>{{ actor.name }}</h1>
    {% for program in actor.programs %}
        <p>{{ program.title }}</p>
    {% endfor %}
{% endblock %}
